# Salesforce login host:
# - prod: https://login.salesforce.com
# - sandbox: https://test.salesforce.com
SF_LOGIN_URL=https://login.salesforce.com

SF_CLIENT_ID=YOUR_CONSUMER_KEY
SF_CLIENT_SECRET=YOUR_CONSUMER_SECRET
SF_REDIRECT_URI=http://localhost:3001/auth/callback

# Must include cdp_query_api for ANSI SQL scope
SF_SCOPES=api refresh_token offline_access cdp_query_api

# Optional: Data Cloud data space name (if your org uses multiple)
# SF_DATASPACE=Human_Resources

# SITE_USER=yourUsername
# SITE_PASS_HASH=<sha256-hex-of-your-password>

SESSION_SECRET=replace-me
PORT=3001

# ---- Data 360 Connect REST API (for Related Objects / lineage) ----
# Auto-discovery probes /services/data/vXX.X/ssot/... on your SF org host.
# Only set these if auto-discovery fails:
#
# DC_CONNECT_BASE_URL=https://YOUR_ORG.my.salesforce.com/services/data/v65.0
# DC_CONNECT_TOKEN=sf
# DC_CONNECT_MAPPING_COLLECTION_PATH=ssot/data-model-object-mappings
# DC_CONNECT_FIELD_REL_COLLECTION_PATH=ssot/field-source-target-relationships
#
# Debug: visit /api/connect/status after login to check SSOT availability

# node -e "console.log(require('crypto').createHash('sha256').update('helzberg2022').digest('hex'))"
